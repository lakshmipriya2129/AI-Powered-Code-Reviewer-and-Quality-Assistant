stages:
  - quality

codeguard-check:
  stage: quality
  image: python:3.11

  before_script:
    - pip install -r requirements.txt

  script:
    - python -m cli.main scan .
    - python -m vcs.precommit

  only:
    - merge_requests
    - main